services:
  - type: web
    name: livo-pronunciation-scorer
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: WHISPER_MODEL_SIZE
        value: base.en
    healthCheckPath: /api/health
